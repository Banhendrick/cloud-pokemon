apiVersion: apps/v1
kind: Deployment
metadata:
  creationTimestamp: null
  name: pokemon-nginx
spec:
  replicas: {{ .Values.replicaCount }}
  selector:
    matchLabels:
      io.kompose.service: nginx
  strategy: {}
  template:
    metadata:
      creationTimestamp: null
      labels:
      # tiene que coincidir con el service
        io.kompose.service: nginx
    spec:
      containers:
      - image:  banhendrick/nginx-php-pokemon:app
        name: nginx-container
        ports:
        - containerPort: 80
        resources: {}
      restartPolicy: Always
status: {}